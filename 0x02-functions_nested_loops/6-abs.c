#include <stdio.h>
#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * Return: An absolue value
 */
int _abs(int n)
{
	int n;

	if (n >= 0)
		return (n);
	else
		return (-n);
}
