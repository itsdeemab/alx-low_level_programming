#include <stdio.h>
#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * Return: An absolue value
 */
int _abs(int)
{
	int n;

	if (n >= o)
		return (n);
	else
		return (-n);
}
